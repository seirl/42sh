#ifndef AUTOCOMP_PATHS_H
# define AUTOCOMP_PATHS_H

# include "terminal.h"

/**
** @brief Return the autocompleted word currently under the cursor.
*/
s_string *autocomp_paths(s_term *term);

#endif /* !AUTOCOMP_PATHS_H */
